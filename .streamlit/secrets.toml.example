# Streamlit Community Cloud — Secrets 예시
#
# 로컬: 이 파일을 secrets.toml 로 복사 후 값 채우기 (또는 .env 사용)
# 배포: Streamlit Cloud 대시보드 > App > Settings > Secrets 에 동일 내용 붙여넣기
#
# secrets.toml 은 .gitignore 에 포함되어 있어 절대 커밋되지 않음.

KEY_DATAGO = "공공데이터포털_Decoding_키"
KEY_FSKR   = "식품안전나라_키"
